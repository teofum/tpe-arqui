Instrucciones para build:

1. ./mkbuildcon.sh para pullear y crear el container de build
2. ./build.sh para compilar y generar la imagen
3. ./run.sh para correr en qemu

./buildrun.sh combina los pasos de build y run
./clean.sh borra los artifacts
